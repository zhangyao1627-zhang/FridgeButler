# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'FridgeButler' do
  use_frameworks!
  pod 'FirebaseCore'
  pod 'FirebaseDatabase'
  pod 'FirebaseFirestore'
end
